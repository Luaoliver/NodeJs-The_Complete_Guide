// 1. Create a npm project and install Express.js (Nodemon if you want)

// 2. Create an Express.js app which funnels the requests through 2 middleware
// functions that log something to the console and return one response.

// 3. Handle request to "/" and "/users" such that each request only has one
// handler/ middleware that does something with it (e.g. send dummy response).

// --------------------

// 1. Crie um projeto npm e instale o Express.js (Nodemon se desejar)

// 2. Crie um aplicativo Express.js que canaliza as solicitações por meio de 2
// funções de middleware que registram algo no console e retornam uma resposta.

// 3. Lidar com a solicitação para "/" e "/users" de forma que cada solicitação
// tenha apenas um manipulador/middleware que faça algo com ela (por exemplo,
// enviar resposta fictícia)
