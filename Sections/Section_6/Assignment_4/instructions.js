// 1. Create a npm project and install Express.js (Nodemon if you want).

// 2. Add two routes:
//      '/' => Holds a <form> that allows users to input their name.
//      'users' => Outputs an <ul> with the users names (or some error text).